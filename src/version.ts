/**
 * Version constant - centralized version management for source code
 * Note: package.json also contains version for npm/build tooling
 * Update both locations when bumping version
 */
export const VERSION = '0.1.0';
